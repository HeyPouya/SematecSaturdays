<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ir.sematec.saturdays">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".TestNotificationActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity android:name=".TestSQLActivity"></activity>
        <activity
            android:name=".TestDialogActivity"
            android:theme="@style/Theme.AppCompat.Dialog" />
        <activity android:name=".TestAlertDialogActivity"></activity>
        <activity android:name=".TestHandlerActivity" />
        <activity android:name=".TestMenuActiviy" />
        <activity android:name=".TestPermissionActivity" />
        <activity android:name=".TestAsyncClientActivity" />
        <activity android:name=".TestHttpURLConnectionActivity" />
        <activity android:name=".TestThreadActivity" />
        <activity android:name=".TestDrawerActivity" />
        <activity android:name=".TestWebViewActivity" />
        <activity android:name=".TestRecyclerActivity" />
        <activity android:name=".TestImplicitIntentActivity" />
        <activity android:name=".TestChildStartForResultActivity" />
        <activity android:name=".TestStartForResultActivity" />
        <activity android:name=".TestEdittextActivity" />
        <activity android:name=".ThirdActivity" />
        <activity android:name=".SecondActivity" />
        <activity android:name=".MainActivity" />


        <service android:name=".TestService" />
        <receiver android:name=".TestBroadcastReceiver">
            <intent-filter>
                <action android:name="android.intent.action.AIRPLANE_MODE" />
            </intent-filter>
        </receiver>
    </application>

</manifest>